[package]
name = "maf_typed"
version = "0.1.0-alpha.4"
edition = "2024"
publish = false

[dependencies]
anyhow = "^1.0.96"
maf_schemas = { path = "../maf_schemas" }
schemars = "1.0.4"
serde = { version = "^1", features = ["derive", "rc"] }
serde_json = "1.0.140"

[dev-dependencies]
maf = { path = "../maf", features = ["typed"] }
