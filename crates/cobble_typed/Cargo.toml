[package]
name = "cobble_typed"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "^1.0.96"
cobble_schemas = { path = "../cobble_schemas" }
schemars = "1.0.4"
serde = { version = "^1", features = ["derive", "rc"] }
serde_json = "1.0.140"

[dev-dependencies]
cobble = { path = "../cobble", features = ["typed"] }
