# MAF Rust `platform` Example

This example is intended to be used alongside the MAF Platform API example
(`packages/platform/examples/api-rooms`) to demonstrate how to create and join
rooms using server-side and MAF Platform APIs.

See the other example's README for instructions on running both examples
together.
