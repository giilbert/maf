[package]
name = "schemas"
version = "0.1.0"
edition = "2024"

[dependencies]
colored = "3.0.0"
rand = "0.9.1"
serde = { version = "^1", features = ["derive"] }
uuid = { version = "1.16.0", features = ["serde", "v4"] }
