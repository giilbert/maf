[package]
name = "cobble-demo"
version = "0.1.0"
edition = "2024"

[dependencies]
cobble = { path = "../../crates/cobble", features = [] }
schemars = "1.0.4"
serde = { version = "^1", features = ["derive"] }

[lib]
crate-type = ["cdylib"]
